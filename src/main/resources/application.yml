server:
  port: 8082
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://${PostgresURL}:5432/postgres
    username: postgres
    password: ${PostgresPW}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
  jackson:
    serialization:
      fail-on-empty-beans: false
feign:
  hystrix:
    enabled: true
    
